# Legacy reference material

`active-theme/` is a complete, read-only reference copy of the legacy `autocar` theme, captured from the LocalWP clone on 2026-07-21. Do not edit, format, regenerate, or treat these files as new production code. All remediation and new work belongs in `theme/` or `plugin/`.

`active-theme/functions.php` remains present locally for the audit but is intentionally excluded from Git because it contains a legacy credential. It must never be copied into a repository or release package.
